<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.adicom.app.mymoney">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".register.RegisterActivity" />
        <activity android:name=".expenses.ExpensesActivity" />
        <activity android:name=".categories.CategoriesActivity" />
        <activity android:name=".addeditexpense.AddEditExpenseActivity" />
        <activity android:name=".addeditcategory.AddEditCategoryActivity"></activity>
    </application>

</manifest>